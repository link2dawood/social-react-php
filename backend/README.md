# PHP Backend - social-media

Auto-generated backend by Lerumos PHP Maker

## Setup Instructions

1. **Requirements**
   - PHP 7.4 or higher
   - SQLite extension enabled
   - Apache/Nginx web server

2. **Installation**
   ```bash
   # Initialize database
   php init.php
   
   # Or use the SQL script directly with SQLite
   sqlite3 database.db < setup.sql
   
   # Set proper permissions
   chmod 755 backend/
   chmod 666 backend/database.db
   chmod 755 backend/uploads/
   ```

3. **Configuration**
   - Edit `config.php` to customize settings
   - Adjust `MAX_UPLOAD_SIZE` and `ALLOWED_EXTENSIONS` as needed

## API Endpoints

### Authentication
- **POST** `/backend/auth.php?action=signup` - Create account
- **POST** `/backend/auth.php?action=login` - Login
- **GET** `/backend/auth.php?action=me` - Get current user
- **POST** `/backend/auth.php?action=logout` - Logout

### File Uploads
- **POST** `/backend/upload.php` - Upload files

### Content
- **GET** `/backend/content.php?action=list` - List items
- **GET** `/backend/content.php?action=get&id={id}` - Get item
- **POST** `/backend/content.php?action=like&id={id}` - Like item

### Messages
- **POST** `/backend/messages.php?action=send` - Send message
- **GET** `/backend/messages.php?action=list` - Get messages
- **POST** `/backend/messages.php?action=mark_read&id={id}` - Mark as read

### Payments
- **POST** `/backend/payments.php` - Process payment

## Security Notes

- Change default database permissions in production
- Use HTTPS in production
- Implement rate limiting
- Add CSRF protection for production use
- Store sensitive config in environment variables

## Database Schema

The database is automatically initialized with tables for:

- Users
- Posts
- Likes
- Messages




## Generated by My PHP Maker
App Type: social-media
Generated: 2026-01-30T06:33:26.357Z
