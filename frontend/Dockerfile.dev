FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Copy source
COPY . .

EXPOSE 5173

# Vite dev server (compose also overrides, but this is a sane default)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

